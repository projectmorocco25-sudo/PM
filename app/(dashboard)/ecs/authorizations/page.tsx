/**
 * Task 1.1.1.12 placeholder. Route: /ecs/authorizations
 * Wireframe: N/A (placeholder). Ref: routing-structure.md
 */

import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function AuthorizationsPage() {
  return (
    <PlaceholderPage
      title="Export Authorizations"
      route="/ecs/authorizations"
      backHref="/ecs"
      backLabel="Back to ECS"
    />
  );
}
