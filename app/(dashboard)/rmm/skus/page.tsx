/**
 * Task 1.1.1.12 placeholder. Route: /rmm/skus
 * Wireframe: N/A (placeholder). Ref: routing-structure.md
 */

import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function SkusListPage() {
  return <PlaceholderPage title="SKUs" route="/rmm/skus" />;
}
