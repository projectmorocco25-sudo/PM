/**
 * Task 1.1.1.12 placeholder. Route: /system-config
 * Wireframe: N/A (placeholder). Ref: routing-structure.md, route-inventory.
 */

import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function SystemConfigPage() {
  return (
    <PlaceholderPage
      title="System Configuration"
      route="/system-config"
      backHref="/dashboard"
      backLabel="Back to Dashboard"
    />
  );
}
